services:
  app:
    build: .
    ports:
      - "${PORT:-5173}:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm dev --host 0.0.0.0
